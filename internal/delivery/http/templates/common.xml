<?xml version='1.0' encoding='UTF-8'?>
<items>
    <title>{{.Title}}</title>
    <background-image>{{.Protocol}}://{{.Host}}/static/mo-home-media.jpg</background-image>
    <channel>
        <title>Поиск</title>
        <search_on>Поиск...</search_on>
        <playlist_url>{{.Protocol}}://{{.Host}}</playlist_url>
    </channel>
    {{- range $item := .Dirs}}
    <channel>
        <title>{{$item.Name}}</title>
        {{- if $item.Path}}
        <playlist_url>{{$.Protocol}}://{{$.Host}}/?source={{$item.Path}}</playlist_url>
        {{- else}}
        <playlist_url>{{$.Protocol}}://{{$.Host}}</playlist_url>
        {{- end}}
    </channel>
    {{- end }}
    {{- range $item := .Paths}}
    <channel>
        <title>{{- $item.Name}}</title>
        <stream_url>{{$.Protocol}}://{{$.Host}}/{{$item.Path}}</stream_url>
    </channel>
    {{- end}}
</items>